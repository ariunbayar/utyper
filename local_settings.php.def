<?php
    $db_host = "localhost";
    $db_name = "";
    $db_username = "";
    $db_password = "";

    // Create table query:
    CREATE TABLE `u_online_users`
    (
        `id` INT NOT NULL AUTO_INCREMENT,
        `session` VARCHAR(100) NOT NULL,
        `time` INT NOT NULL DEFAULT 0,
        `cpm` INT NOT NULL DEFAULT 0,

        PRIMARY KEY(`id`)
    )

    // Create table query:
    CREATE TABLE `u_game`
    (
        `id` INT NOT NULL AUTO_INCREMENT,
        `creator` VARCHAR(100) NOT NULL,
        `topic` INT NOT NULL DEFAULT 0,
        `countdown` INT NOT NULL DEFAULT 0,
        `follower` VARCHAR(100) NOT NULL,

        PRIMARY KEY(`id`)
    )
?>
